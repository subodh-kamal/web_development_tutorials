var num = 5;

console.log(num);

// How can we create a global variable in nodejs
// One way is:

globalThis.num2 = 35; // But this is not a right way to create a global variable.

console.log(num2);
