// ----------------------- map() ---------------------------

/* const numbers = [1, 2, 3, 4];

const doubled = numbers.map((n) => {
  return n * 2;
});

console.log(doubled); */

// Every element is transformed.
// Array length stays the same.

// -------------------------- filter() ----------------------

/* const numbers = [1, 2, 3, 4, 5, 6];

const evens = numbers.filter((n) => {
  return n % 2 === 0;
});

console.log(evens); */

// Removes elements that don't pass the test.
// Array length can shrink(or become empty).

// ------------------- reduce() ---------------------------------------------------
